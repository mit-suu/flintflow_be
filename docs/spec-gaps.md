# Spec gaps

Thiếu sót / mâu thuẫn tài liệu phát hiện khi implement. Ai cũng được **thêm dòng**; không sửa dòng người khác.

| Ngày | Task | Tài liệu | Vấn đề | Cách xử lý tạm |
| --- | --- | --- | --- | --- |
| 2026-09-14 | T03 | Phases §8.2 vs `task-03-skill-assets.md` | Phases đếm 29 file (12 skill content). Task-03 liệt kê 13 content (thêm `product-brief` cho B-0…B-2, T20 sở hữu) ⇒ thực tế 30 SKILL.md (10 đủ nội dung, 20 stub). | Giữ 30; test `prompt-assets.test.ts` khoá số 30. Cần chốt lại con số trong Phases §8.2. |
| 2026-09-14 | T03 | Phases §8.2 | Tên file dạng `draft-to-ops.md` phẳng; task-03 chốt cấu trúc BMAD `action/draft-to-ops/SKILL.md` + `references/`. | Theo task-03. |
| 2026-09-14 | T08 | srs-spine §4.1 | Không nói "tồn tại" nghĩa là gì với khoá section (`sections[].id`, `addendum[].target_section`, `flags[].section_id`). Spine mới có `sections[]` rỗng nhưng addendum đã trỏ `fixed:5.4`. | Khoá section tồn tại khi phân giải được: `fixed:*` thuộc 20 section cố định, `feature:<id>`/`function:<id>` có phần tử. `reference-fields.ts` `sectionKeyExists`. Riêng `addendum[].target_section` chỉ cần đúng dạng (`sectionKeyWellFormed`): fixture minimal có entry trỏ `feature:F3` trước khi feature tồn tại. |
| 2026-09-14 | T08 | srs-spine §3 | Chỉ nêu cascade cho xoá màn/actor/feature. Chưa nói: xoá feature còn màn/function thì sao. | Không tự xoá (restrict): từ chối lô, trả `referrers`. Chính sách từng field ở `cascade.ts` `CASCADE_POLICY` và contract §0.2. |
| 2026-09-14 | T08 | srs-spine §6 | Bất biến 1, 2, 8 viết tuyệt đối thì Spine mới (mảng rỗng) bị từ chối mọi lô; 3–6 tuyệt đối thì Spine migrate có lỗi sẵn không sửa được gì. | 1, 2, 8 so trước/sau lô (chỉ chặn việc xoá); 3–6 chỉ chặn vi phạm mới. Lỗi có sẵn hiện qua cờ đỏ T09. |
| 2026-09-14 | T08 | srs-spine §6 bất biến 5 | `functions[].order` chỉ "duy nhất trong feature", không nói liên tục (fixture T02 lại kiểm liên tục). | Engine chỉ đòi duy nhất; xoá function không renumber (khớp case-03). `renumber functions[]` có sẵn nếu cần. |
| 2026-09-14 | T08 | srs-spine §2 `changes[]` | Không có chỗ lưu vị trí phần tử bị xoá và phân biệt "key không tồn tại" với `null` — revert không khôi phục deep-equal được. | Mã hoá trong `before`/`value`: `{_absent: true}` / `{_absent: true, index}` (op.types.ts `ABSENT`). |
| 2026-09-14 | T09 | srs-spine §5 | `stale` so `changes[].seq` với `accepted_at` (thời điểm) — khác đơn vị. Không nói change do chính step sở hữu ghi có làm stale không. | Mốc = `steps[].last_seq` lớn nhất của step accepted sở hữu s. Change có `step_id ∈ steps_of(s)` không làm s stale (status đã ra draft khi step đó chưa accepted). |
| 2026-09-14 | T09 | srs-spine §5 | `awaiting_reaccept` là "cờ phụ" nhưng không nói lưu ở đâu (lưu thì thành giá trị suy diễn). | Tính từ `steps[]`: có step sở hữu s ở `revision_requested`. T17 hoà giải đặt, gate Accept (T13) xoá. |
| 2026-09-14 | T09 | srs-spine §7.1 / fixture T02 | Fixture để `source_hash = "TBD"`; so hash thật thì mọi hình thành `diagram_stale` đỏ, trái DoD "fixture 0 cờ đỏ". | `source_hash` rỗng hoặc `TBD` = chưa tính ⇒ bỏ qua `diagram_stale`. T10 ghi hash bằng `computeSourceHash`. |
| 2026-09-14 | T09 | srs-spine §7 `section_empty` | Dự án không có non-screen function thì `fixed:3.1.4` luôn rỗng ⇒ cờ đỏ (waive được). Tài liệu không miễn. | Giữ cờ đỏ waive được; cần chốt có miễn `fixed:3.1.4` hay render "N/A". |
| 2026-09-14 | T09 | srs-spine §4 | `functions[].priority` (S-9.4) không có dòng riêng trong bảng §4. | Rơi vào dòng chung `functions[screen_id≠null|null]` ⇒ owner `function:<id>`. Không thêm dòng. |
| 2026-09-14 | T09 | Phases §6.4 / T12 | `steps_of(s)` cần step registry (T12) chưa có. | Bảng tạm `FIXED_OWNER_STEPS` trong `section-registry.ts`; function → `S-5.2`/`S-5.4@<screen|nonscreen>`. Thay khi T12 merge. |
| 2026-09-14 | T10 | srs-spine §2 `entities[].relations` | Chỉ lưu id đích, không có bản số; ERD cần crow's foot. | Mọi quan hệ vẽ `||--o{`. Cần thêm field bản số nếu muốn ERD đúng. |
| 2026-09-14 | T10 | Phases §7.1 context diagram | Nhãn tương tác trên cạnh (payment webhooks…) không thuộc `source_fields` §7.1. | Cạnh không nhãn; nếu cần nhãn phải bổ sung vào `source_fields` và Spine. |
| 2026-09-14 | T10 | plantuml-conventions (T03) vs `prompt-assets.test.ts` | Renderer đã có nội dung nhưng test T03 bắt mọi skill không phải action là `stub: true`. | Giữ `stub: true` + ghi chú trong SKILL.md; XREQ T10→T03 sửa test. |
| 2026-09-14 | T10 | Phases §7.1 usecase > 25 | Không nói id các phần khi tách hình. | `<base>-1`, `<base>-2`…; đổi số phần thì gỡ id cũ trong cùng transaction. |
| 2026-09-14 | T10 | compile-check.ts (giả định "HTTP 200 kèm ảnh lỗi") | Probe thật với `plantuml/plantuml-server:jetty` (1.2026.8): syntax sai ⇒ **HTTP 400** + ảnh lỗi. POST không có header chẩn đoán, GET có `x-plantuml-diagram-error(-line)`. Client cũ coi 400 là lỗi mạng và ném. | Client trả kết quả khi 400 + `image/*`; compile-check dùng header hoặc marker SVG, lấy dòng lỗi từ `[From string (line N)`. Có thể ghim tag 1.2026.8 (TODO(pin) trong docker-compose — T24). |
| 2026-09-14 | T11 | Phases §6.4 / task-11 | "Fields this step may write" cần step registry (T12). Ca op T02 cho thấy step xoá màn (S-4.1) phải gỡ cả `use_cases[].function_ids`, `sections[]`. | Bảng tạm `STEP_SPECS` (context-projection.ts), quyền ghi theo **gốc path mức phase** + `assumptions`. T12 thu hẹp theo step. |
| 2026-09-14 | T11 | skill draft-to-ops | Template không có placeholder cho tài liệu upload. | Nối `documents` vào cuối `content_guidance`; không đổi skill (T03). |
| 2026-09-14 | T11 | skill content (T03 stub) | 12 skill content còn `stub: true` (T14/T18 viết). | `draftOps` gửi hướng dẫn chung thay cho template stub. |
| 2026-09-14 | T11 | usage[] (spine.schema) | Task yêu cầu ghi `contextTokens` vào `usage[]` nhưng `usageSchema` (đóng băng) không có field này. | `draftOps` trả `contextTokens` + `usage[]` theo lượt (tokens_in/out, cost, logId); T13 ghi `tokens_in`. Đổi schema cần `contract-change`. |
| 2026-09-14 | T12 | Phases §4.2 / §6.4 | "Step tất định không Meter" chỉ liệt kê S-8.2, S-8.3, S-9.1, S-9.5; S-5.1 (lấy màn đầu queue), S-5.5 (sign-off), các step render không gọi model nhưng không có trong danh sách. | `deterministic: true` đúng 4 step của §4.2; step render/gate khác để T13 quyết khi có lượt gọi `render_fix`. |
| 2026-09-14 | T12 | Phases §6.2 / §6.4 | Tổng `51 + 5 × N` đếm mọi màn, nhưng màn `placeholder` không chạy vòng S-5 (fixture 19 màn chỉ có 5×6 step). | `totalSteps` giữ công thức tài liệu; `nextStep` bỏ qua vòng của màn placeholder. Thanh tiến độ vì vậy không chạm `total` khi có placeholder — cần chốt có trừ placeholder khỏi N không. |
| 2026-09-14 | T12 | task-12 `reads[]` | Chưa quy ước cách khai "step đọc tài liệu upload". | `reads` chứa token `documents` (thay cho `STEP_NEEDS_SOURCE_DOCUMENTS` tạm của T11 khi hợp nhất). |
| 2026-09-15 | T13 | `step-runner.service.ts` `pendingAnswers`, `meter.service.ts` in-process lock | Hàng chờ `answer_needed`/khoá step (`acquireStepLock`) giữ trong `Map`/`Set` bộ nhớ tiến trình — chỉ đúng khi chạy **một instance** Node. Nhiều instance (scale ngang) sẽ không thấy nhau, mỗi instance tự cho qua trần đồng thời. | Chấp nhận giới hạn single-instance cho tới khi có hàng đợi/khoá phân tán (Redis…) — ghi rõ trong comment đầu `step-runner.service.ts`. Không đổi thiết kế ở T13. |
| 2026-09-15 | T13 | `pipeline-contract.md` endpoint 6 `POST /steps/:stepId/gate`, `gateRequestSchema` | `gateRequestSchema` (đóng băng) không có `session_id` — `/gate` không xác nhận request tới từ session pipeline của project (khác `/run`, `/answer` đều có `NOT_PIPELINE_SESSION`). | Chưa sửa (đổi `gateRequestSchema` cần `contract-change`). `/gate` vẫn kiểm quyền sở hữu project qua `authorize()`. |
| 2026-09-15 | T13 | `pipeline-contract.md` endpoint 5 `POST /steps/:stepId/answer`, `stepAnswerRequestSchema` | `stepAnswerRequestSchema.answers[].answer` (`string \| string[]`, đóng băng) không giới hạn độ dài/số phần tử — client gửi answer khổng lồ vẫn được ghi thẳng vào transcript session. | Chưa giới hạn (đổi schema cần `contract-change`). Rủi ro thấp: chỉ user tự ghi transcript của chính mình. |
| 2026-09-15 | T15 | Product-Brief-to-SRS-Phases.md §6.3 | `functions[].order` là `.y` của số section §3.x.y không được nói rõ là "đánh riêng screen-bound/non-screen" hay là vị trí gộp — đọc thẳng `order` sinh số trùng khi một feature có cả hai loại function cùng `order`. | `.y` = vị trí 1-based trong danh sách đã gộp của feature theo đúng thứ tự `section-registry.listSections` (screen-bound trước, non-screen sau) — không phải `functions[].order` trực tiếp. `assemble.service.buildNumberMap`, test `buildNumberMap` (assemble.test.ts). |
| 2026-09-15 | T15 | srs-spine.md §4 / pipeline.dto.ts `assembleResponseSchema` | `sections` (đếm số section) không nói có tính heading nhóm tổng hợp (`group:*`, chèn lúc assemble, không phải section logic của contract) hay không. | Không tính `group:*` vào `sections` của `assembleResponseSchema` — `RenderedSection.id` có thêm tiền tố `group:` cho heading chương/mục cha (T15); T16 bỏ qua id có tiền tố này khi map flags/steps. Đề xuất ghi nhận tường minh vào `rendered-document.types.ts` qua PR `contract-change` khi có nhu cầu thật. |
| 2026-09-15 | T15 | task-15-assemble-export.md `fixed:I` | "dịch reason theo ngôn ngữ export nếu có" — Spine không lưu ngôn ngữ hiển thị của `reason`/không có nguồn dịch nào ở tầng dữ liệu hiện tại. | Chưa làm (Th3) — `reason` giữ nguyên ngôn ngữ đã ghi lúc tạo change. Cần chốt: dịch máy lúc export, hay bắt buộc ghi `reason` song ngữ lúc tạo change. |
| 2026-09-15 | T14 | `prompt-assets.test.ts` (T03) vs `assets/skills/content/*` | 4 content skill của T14 (project-classifier, product-overview, high-level-rules, actors-and-usecases) đã có nội dung thật nhưng vẫn phải giữ `stub: true` vì test T03 khoá "mọi skill không phải action = stub". | Giữ `stub: true` + ghi chú 1 dòng trong mỗi SKILL.md. XREQ T14→T03: tách assertion thành "action + 4 content skill T14 không stub"; T14 đặt `stub: false` trong cùng PR khi T03 sửa. |
| 2026-09-15 | T14 | `draft-to-ops.ts#contentGuidance` / `prompt-registry.service.ts#getSkill` | `draftOps` gọi `getSkill(ctx.skill)` không truyền `references`, nên `assets/skills/content/actors-and-usecases/references/*.md` không bao giờ tới model — mọi luật bắt buộc (actor kind, đặt tên use case, include/extend, sweep 6 chủ đề) phải nén thẳng vào `SKILL.md` (≤150 dòng) thay vì trỏ sang `references/`. | Nén luật vào `actors-and-usecases/SKILL.md`; `references/*.md` giữ làm tài liệu đọc thêm cho người, ghi rõ "not loaded at runtime". XREQ T14→T11: cho `draftOps` nạp `references: "all"` khi gọi `getSkill` cho skill content, để sau này có thể tách luật ra lại. |
| 2026-09-15 | T14 | `step-registry.json` S-2.5/S-3.1 vs `source-hash.ts#sourceProjection` (context diagram) | S-2.5 render sơ đồ ngữ cảnh từ `actors[kind!=human]`; S-3.1 (chạy sau) được phép thêm actor `kind=system|time`. Thêm actor phi-human ở S-3.1 luôn làm hình ngữ cảnh `diagram_stale` mà không step nào trong S-3 render lại "context" (chỉ S-2.5 render). | Chấp nhận cờ đỏ tạm thời; T14 minh hoạ cách đóng bằng `gate` `revision` + `accept` trên S-2.5 (reopen B7) trong `s2-s3.e2e.test.ts`. Đề xuất contract-change: S-3.6 renders thêm `["usecase", "context"]`, hoặc S-3.1 tự renders `["context"]` sau khi ghi actor. |
| 2026-09-15 | Quyết định Wave 3 | 5 contract-change + 6 XREQ treo sau phiên overnight (T13–T16) | Chặn DoD T13/T14 và M3. | **Đã chốt** (nhánh `wave3/decisions`): (1) `POST /resume` thành endpoint 24; (2) `/gate` bắt buộc `session_id` + kiểm `NOT_PIPELINE_SESSION`, `answers` ≤ 20 phần tử/≤ 4000 ký tự, `note` ≤ 2000; (3) `409 NO_WORKING_DRAFT {hint}` vào bảng lỗi; (4) `RenderedSection.id` `group:*` ghi vào contract; (5) S-3.6 `renders: ["usecase", "context"]` (một điểm render cuối S-3, không thêm render ở S-3.1). XREQ: `refundDeductedCredit` (T04) hoàn ví khi 409; bỏ `stub` cho 4 content skill T14 + 5 renderer T10; `PATCH /users/me {name, onboardedAt}` (modules/user); FE `ChatSession.is_pipeline`, `User.name/onboardedAt`. |
| 2026-09-15 | Quyết định Wave 3 | XREQ T14→T11 nạp `references/` | Nạp `references: "all"` cho skill content làm tăng token mỗi lượt Draft mà luật đã nén đủ trong `SKILL.md`. | **Từ chối**: giữ `SKILL.md` tự đủ (≤ 150 dòng), `references/*.md` chỉ là tài liệu cho người. Xem lại ở T18 nếu skill S-4…S-8 vượt 150 dòng. |
| 2026-09-15 | Quyết định Wave 3 | task-15 `fixed:I` "dịch reason" | Spine không lưu ngôn ngữ của `reason`. | **Chốt**: không dịch lúc export; `reason` giữ nguyên như lúc ghi change. Skill/route tạo change nên ghi `reason` tiếng Anh (luật render tiếng Anh). |
| 2026-09-15 | M3 (chạy thật) | `assemble.service.ts` (T15 review C3) | Sơ đồ chưa có PNG (fixture chưa render) ⇒ `POST /assemble` vẫn 200 nhưng không ghi cache ⇒ `GET /document`/`/export/word` 409 `NO_WORKING_DRAFT` — client không biết vì sao. | Tạm: chạy `POST /diagrams/all/render` trước assemble. Đề xuất T15: trả `images_missing[]` trong `assembleResponseSchema` (contract-change) hoặc cache kèm ảnh placeholder. |
| 2026-09-15 | M3 (chạy thật) | PlantUML local dev | `plantuml.jar -picoweb` trả ảnh "PlantUML PicoWebServer" cho POST `/svg`/`/png` ⇒ compile-check coi là ok, docx nhúng ảnh giả. | Dev không Docker: dùng shim chuyển tiếp GET `/svg|/png/<enc>` sang `/plantuml/...` và trả 405 cho POST. Production vẫn dùng `plantuml-server` (T24). |
| 2026-09-15 | M3 (chạy thật) | `glm.provider.ts` (GLM-5.3-Flash trên Modal) | `reasoning_effort: "none"` không tắt thinking ⇒ `content` có phần suy nghĩ + `</think>` ⇒ elicit `PARSE_FAILED` ngay S-1.2. | Sửa `db0defe`: gửi `thinking: {type: "disabled"}` + `stripReasoning`. |
| 2026-09-15 | Quyết định model | Skill S-2/S-3 (và mọi skill `provider: glm`) | Endpoint Modal chỉ có `zai-org/GLM-5.3-Flash`; `thinking: disabled` vẫn suy nghĩ ngầm tới hết token ở S-3.2 (run8 dừng 7/12). | **Chốt giữ GLM-5.3-Flash** (user 2026-09-15), gửi `reasoning_effort: "low"` + `response_format: json_object` (`7618fe7`, thay cách `thinking: disabled`). Probe prompt S-3.2: 13 s, ~250 token suy nghĩ, lô op hợp lệ. |
| 2026-09-15 | M3 (trình duyệt) | FE `ExportPanel.tsx` gọi `GET /projects/:id/baselines` (endpoint 20, T19) | BE chưa mount route ⇒ Express trả 404 HTML ⇒ panel hiện đỏ "Không kiểm tra được baseline: HTTP 404: Server returned non-JSON response". Không chặn tải bản nháp; tab "Word baseline" bị khoá. | Chờ T19 (Wave 4) hiện thực endpoint 20. Nếu cần sạch sớm: BE trả `[]` cho endpoint 20 trước khi có baseline. |
| 2026-09-15 | M3 (trình duyệt) | `export/word` + CORS (`src/app.ts`) | FE không đọc được `Content-Disposition` (CORS không `exposedHeaders`) ⇒ file tải về dùng tên dự phòng `M3 S-2_S-3 real provider (run18)-v0.58-draft.docx` thay tên BE `m3-s-2-s-3-real-provider-run18-v0.58-draft.docx`. | Đề xuất T24/T15: thêm `exposedHeaders: ["Content-Disposition"]` vào cấu hình CORS. |
| 2026-09-16 | T18 | `assets/step-registry.json` S-5.1, S-5.5 (đóng băng M2) | Hai step chỉ là sổ sách của vòng lặp (chọn màn kế tiếp, cổng ký duyệt) nhưng `deterministic: false` và S-5.1 còn được ánh xạ skill `function-detail` ⇒ mỗi màn tốn thêm 2 lượt gọi model cho việc không cần model. Đổi `deterministic` cần PR `contract-change`. | Runner bỏ qua Draft cho hai template này (`LOOP_BOOKKEEPING_TEMPLATES`, `step-runner.service.ts`); registry giữ nguyên. Nếu sau này mở PR `contract-change` thì đổi `deterministic: true` và gỡ hằng số này. |
| 2026-09-16 | T18 | Phases §6.2 "để lại màn" vs `gateRequestSchema` (đóng băng) | Không có hành động gate nào nghĩa là "để lại màn này, làm sau" — chỉ có accept / revision / regenerate / accept_as_is. | `accept_as_is` trên `S-5.1@<màn>` đặt `screens[].detail_status = placeholder` (`gate.service.loopScreenStatusOps`). Hợp lý về ngữ nghĩa: `accept_as_is` bắt buộc `note` và mở cờ vàng `accepted_as_is` — đúng là lời giải thích "vì sao để lại". Màn giữ nguyên khung `functions[]` nên không bắn `screen_pending_at_baseline`. |
| 2026-09-16 | T18 | `draft-to-ops.ts` `promptVariables` (vùng T11) | Chia lô ≤ 6 function cần nói cho model biết lô nào, nhưng thêm biến prompt phải sửa file của T11. | Thu hẹp chính `projection` của lượt đó (`step-runner.batchContext`) thay vì thêm biến: model chỉ **thấy** function trong lô. Bảo đảm mạnh hơn là dặn nó đừng đụng function khác. Không cần XREQ. |
| 2026-09-16 | T18 | srs-spine §1 op grammar vs task-18 "S-5.4 ghi `validations[]`" | `functions[].validations` là mảng phần tử có `id` ⇒ `set` cả mảng bị từ chối (`op_not_allowed`); phải `add` từng phần tử. Task-18 và bản nháp skill đều viết `set`. | Skill `function-detail` nêu rõ một `add` cho mỗi validation, fixture `s-5.4.json` làm đúng vậy. Phát hiện khi chạy e2e, không phải suy đoán. |
| 2026-09-16 | T18 | `assets/step-registry.json` S-7.1 `writes` vs traceability §4.1 | S-7.1 chỉ được ghi `business_rules`, `assumptions` nên KHÔNG thể nối ngược `functions[].business_rule_ids`. | Cạnh truy vết của §5.1 là `business_rules[].source_validation_ids` (một chiều). `functions[].business_rule_ids` chỉ được ghi ở S-5.4 cho rule đã tồn tại từ trước (tier `high` của S-2.4). Ghi rõ trong cả hai skill. |
| 2026-09-16 | T18 | `prompt-assets.test.ts` (T03) | Test khoá "mọi skill không phải action = stub"; 8 content skill của T18 viết thật thì phải bỏ `stub`. | XREQ T18→T03 theo đúng tiền lệ đã chốt 2026-09-15 cho T10/T14: thêm 8 thư mục vào `WRITTEN_NON_ACTION`. Chỉ còn `content/product-brief` là stub (T20 viết). |
| 2026-09-16 | T18 | `skills/*.e2e.test.ts` nhánh `E2E_AI=1` (khung do T14 đặt, T18 dùng lại) | Nhánh này chỉ thay executor sang `executeAiAction` thật, nhưng `executeAiAction` cần **Mongo thật** (`reserveCreditWithTransaction` → `CreditWallet`/`CreditLedger`, rồi `AiActionLog.create`) mà tiến trình vitest không nối Mongo (`vitest.config.ts` không có `setupFiles`, test chỉ mock `Spine/Change/Usage/ChatSession`). Chạy `E2E_AI=1` sẽ treo ở mongoose buffering rồi timeout, không phải lỗi provider. Đúng với cả `s2-s3.e2e.test.ts` (T14) — chưa ai chạm vì chưa ai chạy. | Chưa sửa (khung là của T14/T22). Ba hướng: (a) nối Mongo thật trong `beforeAll` của nhánh `E2E_AI` và dùng user/project có thật; (b) mock thêm `CreditWallet`/`CreditLedger`/`AiActionLog` — nhưng thế thì không còn đo được credit thật; (c) **cách T14 đã thật sự dùng cho run12–20**: chạy qua API trên BE + Mongo thật thay vì qua vitest. Đề xuất chốt (c) và sửa lời chú thích trong hai file e2e cho khỏi hiểu nhầm. |
| 2026-09-16 | T17 | `task-17-change-flow.md` "ghi `changes[] {op: "undo"}`" | `op.types.ts` (đóng băng) không có op `undo`; contract §0.2 quy định revert ghi `op: "revert"`. Thêm giá trị mới = `contract-change`. | Undo dùng `revertRange` sẵn có ⇒ change mang `op: "revert"` + `reason: "Revert seq N"`. `findUndoTarget` nhận diện lô undo qua chính `reason` này để undo hai lần liên tiếp đi lùi đúng thứ tự. |
| 2026-09-16 | T17 | `pipeline-contract.md` §0.3 | Không có mã lỗi cho "`preview_id` hết hạn / đã dùng / của project khác". | Dùng lại `422 CHANGE_RANGE_INVALID` — đúng mã mà mock FE (T16) đã trả cho tình huống này. Tên mã lệch nghĩa; nếu đổi thì phải sửa cả mock + FE trong PR `contract-change`. |
| 2026-09-16 | T17 | srs-spine §9 impact query | Không nói impact có bắt quan hệ **ngữ nghĩa** không (tên actor nằm trong văn xuôi `use_cases[].description`). | Impact chỉ đi theo `reference_fields[]` + bảng §4 — là **cảnh báo heuristic**, ghi rõ ở đầu `impact.service.ts` và hiện ra UI (T16). Quan hệ trong văn xuôi để S-8.4 Consistency Pass bắt. |
| 2026-09-16 | T17 | srs-spine §9 "3 nhánh" | Không nói `impact.diagrams` là "hình có thể liên quan" hay "hình sẽ phải vẽ lại". | Tính bằng so `source_hash` trước/sau lô (`diagramsToRerender`) — đúng danh sách hình phải vẽ lại, khớp nhãn "Diagram render lại" của `DiffPreviewModal` (T16). Hệ quả: đổi tên actor `kind=human` chỉ vẽ lại `usecase` (hình ngữ cảnh chỉ chiếu actor phi-human), không phải 2 hình như task-17 phỏng đoán. |
| 2026-09-16 | T17 | `change.service.ts` kho `preview_id` | Bản xem trước giữ trong `Map` bộ nhớ tiến trình (như `pendingAnswers`/khoá step của T13) ⇒ chỉ đúng khi chạy một instance Node; TTL 15 phút. | Chấp nhận giới hạn single-instance như T13. `preview_id` mất hiệu lực ⇒ 422, user xem lại diff rồi xác nhận lại — không có mất mát dữ liệu. |
| 2026-09-16 | T17 | srs-spine §5 vs UC 6.10 | User sửa thẳng một field qua chat ⇒ section **sở hữu** field đó cũng thành `stale` (change không mang `step_id` của step sở hữu), không chỉ section "đọc". | Đúng theo §5 và `section-status.ts` (T09) — hoà giải vì vậy cũng đề xuất ops cho chính section sở hữu. Ghi lại để không ai coi là lỗi. |
| 2026-09-16 | T17 | Phases §2.3 "sửa qua chat" | Không có tiêu chí "tin nhắn nào là lệnh sửa" cho session **không** pipeline. | `isChangeInstruction` (change.service.ts): động từ sửa trong 4 từ đầu và câu không phải câu hỏi. Cố ý hẹp — nhận nhầm câu hỏi thành lệnh sửa vừa tốn credit vừa làm user hoảng. Không nhận ⇒ vẫn là CHAT thường. |
| 2026-09-16 | T19 | `section-registry.ts` bảng §4 / spec-gap T09 "`functions[].priority` rơi vào dòng chung" | Hệ quả chỉ lộ ra khi chạy thật: S-9.4 ghi `priority` cho **mọi** function và NFR, mà dòng chung `functions_*` có `reads = fixed:3.1.2, fixed:2.2.2` ⇒ toàn bộ section đọc `functions[]` thành `stale`, rồi `section_stale_at_baseline` chặn đúng cái baseline ngay sau đó. Đo trên fixture 19 màn: **62 cờ đỏ** sinh ra chỉ vì xếp hạng. | `sectionsOfPath` trả rỗng cho `functions[].priority` và `nfrs[].priority` — cùng cách đã làm sẵn cho `functions[].order`: đây là siêu dữ liệu render thành một cột, không đổi nội dung section nào. Bảng §4 không đổi một dòng. Cần 4/4 xác nhận nếu nhóm coi đây là chạm hợp đồng. |
| 2026-09-16 | T19 | `task-19-s9-baseline.md` "409 `RED_FLAGS_OPEN`" vs `pipeline-contract.md` §0.3 | Hợp đồng đã có sẵn `422 BASELINE_BLOCKED` kèm `meta.flags[]` cho đúng tình huống này; task đề xuất một mã khác. | Theo hợp đồng: `422 BASELINE_BLOCKED`. Không thêm mã mới để khỏi phải mở PR `contract-change` cho một thứ đã có. |
| 2026-09-16 | T19 | `flags.service.planFlagOps` | Cờ KHÔNG do deterministic check sinh (`accepted_as_is` của gate — T13, `goal_not_covered` của S-9.3 — T19) bị chính lượt recompute kế tiếp đóng, vì không có ứng viên tương ứng. Nghĩa là cờ biến mất ngay bước sau, chưa ai kịp đọc. Lỗi này đã tồn tại từ T13 với `accepted_as_is`. | Thêm `MODEL_OWNED_RULES` (`deterministic-check.ts`); `planFlagOps` bỏ qua chúng khi dọn cờ. Muốn đóng thì đóng có chủ đích. |
| 2026-09-16 | T19 | `task-19` "Route `GET /projects/:id/baselines/:baselineId/document`" | Endpoint 16 của hợp đồng đã làm đúng việc đó: `GET /projects/:id/document?source=baseline&baseline_id=<id>` (T15 đã hiện thực). Thêm alias = thêm endpoint ngoài hợp đồng (coding-rules §3.8). | Không thêm alias. Chỉ mount hai endpoint có trong hợp đồng: 19 `POST /baseline`, 20 `GET /baselines`. |
| 2026-09-16 | T19 | `flintflow_fe/lib/api/export.ts` `createBaseline` | FE gọi `POST /projects/:id/baseline` **không có body**, nhưng `baselineRequestSchema` (đóng băng) bắt buộc `base_version` ⇒ BE trả 400. FE sẽ không ký được baseline cho tới khi sửa. | BE làm đúng hợp đồng. XREQ T19→T16: `createBaseline(projectId, baseVersion)` gửi `{ base_version }`; đổi cả `ExportPanel` truyền `spine_version` đang xem. Một-hai dòng, chưa làm vì `lib/api/export.ts` là vùng của T16. |
| 2026-09-16 | T19 | `assets/step-registry.json` S-9.3, S-9.4 vs `STEP_SKILLS` (`context-projection.ts`, vùng T11/T14) | Hai step này cần skill nhưng `STEP_SKILLS` không ánh xạ step S-9, và T19 chỉ có quyền đọc file đó. | `s9/prioritization.ts` dựng `ctx` từ `buildStepContext` rồi **ghi đè `ctx.skill`** trước khi gọi `draftOps`; `s9/goal-validation.ts` gọi thẳng skill action `review-section` qua `ActionType.REVIEW`. Không sửa file ngoài vùng. Nếu sau này muốn gọn hơn thì thêm hai dòng vào `STEP_SKILLS` qua XREQ. |
| 2026-09-16 | T20 | `assets/step-registry.json` B-2.2 `writes` vs `task-20` "Addendum Triage (giữ / để dành / bỏ)" | "Để dành" trong task nghĩa là chuyển addendum thành `other_requirements[kind=assumption]`, nhưng registry chỉ cho B-2.2 ghi `addendum`, `assumptions` — op bị `op-validator` chặn. | "Để dành" = **đổi `target_section` sang `fixed:5.4`**; S-7.4 (T18 `appendix-content`) đọc addendum nhắm `fixed:5.4` và biến thành Other Requirement. Thông tin không mất, không cần đổi registry, và ngược lại được. Ghi vào skill, reference, fixture và panel FE. |
| 2026-09-16 | T20 | `context-projection.ts` `STEP_SKILLS` (vùng T11) | S-1.1/S-1.3/S-1.4 đang trỏ `project-classifier` — skill phân loại dự án, không phải skill đọc lại Brief. T20 chỉ có quyền đọc file đó. | XREQ T20→T11 (2 dòng): `S-1.2` giữ `project-classifier`, ba step còn lại trỏ `brief-analysis` (skill mới của T20). Không có ánh xạ này thì skill mới không bao giờ tới model. Cần `granted` trước khi merge. |
| 2026-09-16 | T20 | `assets/skills/content/product-brief` frontmatter (T03 đặt) | `output_schema: discoveryStep` không đúng cách skill content được dùng: `draftOps` ghép nội dung skill vào lượt `DRAFT` (schema `opTransaction`), còn `discoveryStep` là schema của `ActionType.DISCOVERY_STEP` đi với skill **action** `elicit-loop`. | Đổi thành `opTransaction` cho khớp thực tế. Nếu sau này muốn B-* trả vừa `reply` vừa `ops` trong một lượt thì phải đổi ở runner (dùng `DISCOVERY_STEP` thay `ELICIT` + `DRAFT`), lúc đó mới quay lại `discoveryStep`. |
| 2026-09-16 | T20 | `task-20` DoD `grep -n "evaluation" flintflow_fe/app/projects` rỗng | Muốn giữ một ghi chú giải thích **vì sao** bốn tầng parse cũ bị bỏ thì câu đó lại chứa đúng từ khoá bị cấm. | Ghi chú giữ nguyên nhưng diễn đạt là "khối tự chấm tiến độ" thay vì dùng lại tên trường cũ. Grep rỗng thật, không phải né kiểm. |
| 2026-09-16 | T20 | `assets/prompts/chat_discovery.md` | Prompt Discovery cũ (6 bước, LLM tự chấm `evaluation`) không còn đường gọi nào sau khi bỏ `ActionType.CHAT_DISCOVERY` khỏi `chat-session.service`. | Để nguyên file; nội dung 6 bước đã chuyển vào `content/product-brief`. `ActionType.CHAT_DISCOVERY` vẫn còn trong enum (đánh dấu `@deprecated`) — xoá cả hai ở T21. |
| 2026-09-16 | T21 | `task-21` cascade khi xoá cứng project | Task ghi cascade `Notification`, `PaymentIntent` nhưng cả hai model không có `projectId` — thuộc user (lịch sử thanh toán, credit). | Không xoá hai collection này. Thay vào đó dọn thêm `RenderedDocumentCache`, file sơ đồ GridFS và asset Cloudinary của tài liệu. |
| 2026-09-16 | T21 | `task-21` migration — `changes[] {op:"migrate", before: null}` | Engine ghi `before` của op `migrate` là Spine trước lô (không phải `null`) để revert được. | Giữ hành vi engine: một change `migrate`, `reason: "legacy import"`, `by: "system"`, `before` = Spine rỗng. |
| 2026-09-16 | T21 | `task-21` migration `use_case_spec` | Task chỉ đưa `use_case_spec` vào addendum khi bảng không parse được; nhưng bảng chỉ mang id/tên/actor, còn luồng/tiền-hậu điều kiện nằm ở văn bản. | Luôn giữ văn bản gốc ở addendum `fixed:2.2.2`; parse được thì thêm `actors[]` + `use_cases[]`. |
| 2026-09-16 | T21 | Dữ liệu `sections` trên dev | Nhiều section cũ bắt đầu bằng suy nghĩ của model ("The user wants me to write…"), bảng thiếu dòng phân cách header, mục tiêu viết thành bảng/heading `BG-xx` thay vì bullet. | Parser chịu bảng thiếu dòng phân cách, goals đọc mã `BG-xx` trước rồi mới tới bullet; văn bản giữ nguyên trong addendum để B-2.2 Addendum Triage dọn. |
| 2026-09-16 | T21 | `task-21` bảng ánh xạ migration | Task không nêu đích cho `scope_out_of_scope`, `priority_ranking`, `success_metrics`, `non_functional_requirements` (4.2.x có 4 mục). | `scope_out_of_scope` → `fixed:1`, `priority_ranking` → `fixed:3.1.2`, `success_metrics` → `fixed:5.4`, NFR → `fixed:4.2.4`. Bảng đầy đủ ở `docs/architecture.md` §9. |
| 2026-09-16 | T21 | `project.model.ts` | Gỡ field `currentStep/currentPhase/…/progressPercent` khỏi schema nhưng document Mongo cũ vẫn còn giá trị (schema strict bỏ qua khi đọc). | Không `$unset` trong migration. FE `ProjectCard` + `mocks/state.ts` vẫn đọc `currentStep/progressPercent` (vùng T23) — XREQ T21→T23 chuyển sang tiến độ Spine. |
| 2026-09-16 | T21 | `ai-action.types.ts` ("đổi phải qua contract-change") | Gỡ 6 `ActionType` deprecated là việc task-21 ghi rõ, không phải đổi tên/ý nghĩa giá trị đang dùng. | Gỡ trong T21, không mở PR contract-change; `AiActionLog` cũ vẫn có thể mang tên cũ (admin chỉ nhóm theo chuỗi). |
| 2026-09-16 | T23 | `task-23` "khởi BE với `AI_PROVIDER=mock`" vs `shared/ai/providers/` | Không có biến môi trường nào ghi đè provider — provider chọn theo frontmatter từng skill (`provider: glm`). Và `mock.provider.ts` trả JSON cố định (`{status, message, promptSnippet}`) không khớp `opTransactionSchema`/`elicitSchema`, nên ép dùng mock thì step chết ở bước parse chứ không sinh op. Nghĩa là **không thể chạy một step pipeline trong e2e/CI mà không gọi model thật**. | e2e của T23 gieo nội dung bằng op qua `POST /changes` (không cần model) — vẫn đi qua op engine và invariants thật, vẫn kiểm được seam FE↔BE. Để e2e phủ cả bước AI thì cần: (a) `AI_PROVIDER` ghi đè provider của mọi skill, và (b) `mock.provider.ts` trả output hợp schema theo `ActionType`. Thuộc T22 (test tích hợp) hoặc T24 (env/ops). Cùng nguyên nhân với dòng T18 về nhánh `E2E_AI=1`. |
| 2026-09-16 | T22 | `pipeline-contract.md` endpoint 16/18 `baseline_id` vs endpoint 19/20 `Baseline.id` | `POST /baseline` và `GET /baselines` trả `id: "BL001"`, nhưng `GET /document?source=baseline&baseline_id=` và `GET /export/word` tra `Baseline._id` Mongo (= `snapshot_ref`) — `baseline_id=BL001` ⇒ `404 BASELINE_NOT_FOUND`. Client đọc danh sách baseline rồi mở tài liệu theo `id` sẽ luôn 404. FE chưa truyền `baseline_id` ở đâu nên chưa lộ ra UI (T23 sẽ nối). Phát hiện bằng `test/integration/baseline-export.int.test.ts`. | T22 không sửa (vùng `render/` chỉ đọc). Test đi theo hành vi hiện tại (`snapshot_ref`) kèm chú thích. Cần chốt một trong hai: (a) `assemble.service.getBaselineDocument` nhận cả `id` "BLnnn" — sửa trong vùng T15, không đổi hợp đồng; (b) ghi rõ trong hợp đồng rằng `baseline_id` là `snapshot_ref`. Đề xuất (a). |
| 2026-09-16 | T22 | spec-gap T18 "nhánh `E2E_AI=1` không nối Mongo" (dòng trên) | Khung mới của T22 gỡ đúng nguyên nhân: project vitest `e2e-ai` và `integration` dùng `test/setup.ts` (Mongo in-memory replica set) nên `executeAiAction` reserve/trừ credit thật được. Chứng minh bằng mock ở tầng `callLLM`: `pipeline-s3.int.test.ts` chạy S-1.2 → S-3.6 qua HTTP, ví trừ đúng tổng `Usage.cost`, lỗi provider hoàn đủ credit. | Không sửa hai file `skills/*.e2e.test.ts` (vùng T14/T18, và hướng (c) đã chạy thật). Lượt chạy provider thật mới nên dùng `npm run test:e2e-ai` hoặc `npm run measure:tokens -- --mode real-draft` thay vì gõ tay qua API. |
| 2026-09-16 | T22 | `fixtures/spine-fixture-19-screens.json` (T02) | "Fixture 19 màn" có **14/19 màn `placeholder`** ⇒ chỉ 6/20 vòng S-5 được chi tiết (5 màn + `@nonscreen`), `nextStep` bỏ 70 step. Số đo "19 màn" vì thế thấp hơn một project 19 màn làm chi tiết hết. | `measure-tokens` ghi cả số đo thật (6 vòng) lẫn dòng **ngoại suy 20 vòng** (trung bình một vòng × 20). Nếu nhóm muốn số đo trực tiếp thì T02 cần một fixture có mọi màn `signed_off`. |
| 2026-09-16 | T22 | `docs/measurements.md#threshold` (task-22 "ngưỡng do nhóm đặt") | Tài liệu chưa có ngưỡng nào; `admin.service.PROVIDER_USD_PER_1K` không có đơn giá cho `glm` (provider đang dùng) nên không quy ra USD được. | Thêm mục `## Threshold` trống để nhóm điền; script đọc và so nếu có. USD chỉ tính khi truyền `--usd-in`/`--usd-out`. Báo cáo T22 ghi "chưa đặt" / "n/a", không tự đặt số. |
| 2026-09-16 | T24 | spec-gap T23 "khởi BE với `AI_PROVIDER=mock`" (dòng trên) | T24 làm nốt cả hai vế còn thiếu: (a) `AI_PROVIDER_OVERRIDE` trong `env.ts` ghi đè provider của **mọi** skill ở `llm.router.callLLM`; (b) `mock.provider.ts` trả output hợp schema theo `ActionType` (`{ops: []}` cho draft-family, `{reply, questions: []}` cho elicit, `{flags: []}` cho review, `{puml}` cho render_fix, `{clarification_needed}` cho change_instruction). | Kiểm bằng lượt chạy thật: compose với `AI_PROVIDER_OVERRIDE=mock`, `npm run run:pipeline -- --until B-0.2` đi trọn 2 step (2 lượt gọi/step, 0.3–0.4 s, không ra mạng). Lô op rỗng là **cố ý** — mock không đọc được Spine nên không thể sinh op hợp bất biến; nó chứng minh đường đi, không chứng minh nội dung. Kịch bản e2e của T23 vẫn gieo nội dung bằng op; muốn e2e phủ luôn bước AI thì giờ đã có đường. |
| 2026-09-16 | T24 | `assets/step-registry.json` B-2.3 `reads: ["project","other_requirements","assumptions[status=unconfirmed]"]` vs `context-projection.ts` | Projection chỉ thêm danh sách id `assumptions` khi step **không** đọc `assumptions`. B-2.3 đọc mảng đã **lọc**, nên mọi assumption vừa được B-2.1 xác nhận biến mất khỏi context và model đặt trùng id (`AS10 đã tồn tại`, hỏng 3/3 lượt ⇒ `NEEDS_USER_INPUT`) — chặn đứng lượt chạy M4. | Sửa ở `context-projection.ts` (không đụng registry đóng băng): chỉ bỏ qua khi step đọc **trọn** mảng (`assumptions` hoặc `assumptions:<khoá>`); `assumptions[<lọc>]` vẫn được thêm danh sách id. Có test hồi quy. |
| 2026-09-16 | T24 | `pipeline-contract.md` `GET /progress` · `POST /resume` → `progress.current_step` | Trường này chỉ được ghi lúc `/run` **bắt đầu** (`step-runner.service.ts`), không phải lúc gate accept. Sau khi accept một step mà chưa chạy step kế, `current_step` vẫn trỏ vào step **đã accepted** — client tin vào nó rồi gọi `/run` sẽ nhận `STEP_NOT_RUNNABLE`. Gặp khi chạy tiếp một project dở bằng `run-full-pipeline --project`; FE chưa lộ vì trong phiên nó dùng `next_step` của gate. | T24 không đổi hợp đồng: `run-full-pipeline.ts` tự tính step tới lượt theo đúng luật `nextStep()` (step đầu tiên chưa accepted, bỏ vòng S-5 của màn `placeholder`). Cần nhóm chốt: (a) gate ghi luôn `progress.current_step` = next step, hoặc (b) `buildProgressReport` trả `nextStep(spine)?.id` — (b) sạch hơn nhưng kéo `spine/section-status.ts` phụ thuộc `pipeline/step-registry.ts`. |
| 2026-09-16 | T24 | `flintflow_fe/app/layout.tsx` — `NEXT_PUBLIC_GOOGLE_CLIENT_ID` | Layout luôn bọc app trong `GoogleOAuthProvider clientId={... \|\| ""}`. Thiếu biến (đúng cảnh build image quên `--build-arg`) ⇒ script `gsi/client` khởi tạo với client id rỗng và ném lỗi ⇒ **cả app thành "This page couldn't load"**, kể cả đăng nhập bằng mật khẩu. Không thấy trên `next dev` vì máy dev có `.env.local`. Phát hiện khi chạy e2e T23 trong docker compose. | `lib/google-auth.ts`: không có client id thì **không bọc** provider và `GoogleButton` trả `null` (tách hai component vì `useGoogleLogin` là hook). Đăng nhập Google thành tuỳ chọn thay vì điều kiện sống còn. Compose/Dockerfile nhận `NEXT_PUBLIC_GOOGLE_CLIENT_ID` qua build arg. Có test. |
| 2026-09-16 | T24 | `package-lock.json` của **cả hai** repo | `npm ci` **hỏng trên Linux**: lockfile sinh trên Windows thiếu optional dependency chỉ có ở nền tảng khác (`@emnapi/runtime`, `@emnapi/core` ở FE; `yaml` ở BE) ⇒ `EUSAGE ... lock file is not in sync`. Nghĩa là `backend-ci`, `frontend-ci` và mọi image Docker đều chết ngay ở bước cài. | Sinh lại lockfile trong container Linux (`npm install --package-lock-only`) — chỉ thêm entry thiếu và bỏ cờ `peer`, **không** đổi version nào. Lần sau chạm dependency nên sinh lockfile trong Linux, hoặc để CI cài lại. |
| 2026-09-17 | FLF-163 | `pipeline-contract.md` endpoint 16/18 `baseline_id` vs 19/20 `Baseline.id` (spec-gap T22 phía trên) | Chốt phương án (a): `assemble.service.getBaselineDocument` nhận cả mã `BLnnn` lẫn `_id` Mongo; contract không đổi. | `resolveBaselineObjectId`: `_id` hợp lệ ⇒ tra thẳng; `BL\d+` ⇒ đổi sang `_id` qua `Spine.baselines[].snapshot_ref` (Spine chỉ dùng để đổi mã, nội dung vẫn dựng từ `Baseline.snapshot`); mọi trường hợp khác (mã lạ, `snapshot_ref` không phải ObjectId — schema chỉ ràng chuỗi) ⇒ 404 `BASELINE_NOT_FOUND`, không để `CastError` thành 500. Test unit + `baseline-export.int.test.ts` phủ cả hai dạng id. |
| 2026-09-17 | FLF-164 | `src/app.ts` CORS (spec-gap M3 trình duyệt phía trên) | Thêm `exposedHeaders: ["Content-Disposition"]` vào `cors()` — FE đọc được tên file `.docx` BE đặt; whitelist origin không đổi. Test `test/integration/cors.int.test.ts`. | Còn hở cùng loại nhưng ngoài ticket: `X-Assembled-At-Version`, `X-Spine-Version` mà `export.controller.ts` gửi kèm `.docx` (độ mới bản draft) cũng không nằm trong safelist ⇒ FE chưa đọc được qua `fetch`. Nhóm chốt có expose thêm hai header này không. |
| 2026-09-17 | FLF-165 | `pipeline-contract.md` `GET /progress` · `POST /resume` → `progress.current_step` (spec-gap T24 phía trên) | Chốt hướng (b) nhưng tính ở tầng pipeline: `pipeline-progress.ts` bọc `buildProgressReport` và thay `current_step = nextStep(spine)?.id ?? null` (step đầu tiên chưa accepted, bỏ vòng S-5 của màn placeholder). `section-status.ts` không đổi, không phụ thuộc `step-registry`. Shape response không đổi; con trỏ `Spine.progress.current_step` vẫn chỉ ghi lúc `/run`. | Chưa đổi (để nhóm chốt): (1) `current_phase` vẫn là giá trị lưu trong Spine ⇒ ngay sau khi accept step cuối của một phase, `current_phase` lệch với `current_step` cho tới lượt `/run` kế; (2) `GET /steps` (`pipeline.controller.ts`) vẫn trả con trỏ Spine — FE `page.tsx` dùng chuỗi `??` nên khi `current_step = null` (hết step) sẽ rơi xuống giá trị cũ; (3) Spine **thiếu bản ghi step** (fixture 19 màn không có 13 step B-*) ⇒ `nextStep()` = `B-0.1` dù con trỏ ghi S-9.5 — đúng luật, nhưng client tin vào nó sẽ chạy lại Brief; project thật luôn có bản ghi vì mọi step đi qua `/run`. Test: `pipeline-progress.test.ts`, `resume.test.ts` (accept tuần tự ⇒ step kế), `flags.int.test.ts`. |
| 2026-09-17 | FLF-166 | `assemble.service.ts` — sơ đồ `render_status=ok` thiếu PNG (spec-gap M3 chạy thật phía trên) | Không đổi hợp đồng: section luôn sinh ảnh dạng tham chiếu `diagram-ref:<id>`, cache **luôn** được ghi (draft lẫn baseline); lúc trả về mới phân giải — PNG tải được ⇒ ảnh thật, không ⇒ PNG placeholder (`diagram-placeholder.ts`, sinh bằng `zlib`) + caption "image pending: diagram <id> has not been rendered yet". `POST /assemble` trả finding `diagram_png_missing` qua `meta.consistency` (cả khi trúng cache — id thiếu lưu ở `rendered_documents.missing_diagram_ids`). PNG có sau (render lại `force`, khôi phục file store) ⇒ lần đọc kế có ảnh thật, không cần `spine_version` mới; baseline đã ký không bị placeholder vĩnh viễn. Thay quyết định T15 review C3/Th4 ("thiếu ảnh ⇒ không cache"); bỏ `stripImagesForCache` (đảo map theo byte, lẫn id khi hai ảnh cùng byte). | Còn treo: (1) `meta.consistency` chưa có trong `pipeline-contract.md` (T15 thêm khi review) ⇒ FE chưa dựa vào được cho tới khi có `contract-change`; (2) `GET /document` / `GET /export/word` chưa mang tín hiệu ảnh thiếu ngoài caption trong tài liệu — thêm `meta.missing_diagrams` là additive nhưng ngoài ticket; (3) `rehydrateImages` nay giữ block + placeholder thay vì bỏ ảnh của sơ đồ đã xoá sau khi cache (bản draft khi đó đã `stale`). |
| 2026-09-17 | FLF-167 | Chú thích `skills/s2-s3.e2e.test.ts`, `s4-s8.e2e.test.ts` + mục Test của `CLAUDE.md` (spec-gap T18, T22 phía trên) | Chỉ sửa chú thích/tài liệu, không đổi logic test. Hai file e2e thuộc project vitest `unit` (`src/**`, không Mongo) nên nhánh `E2E_AI=1` trong đó vẫn không chạy được và **không** chạy dưới `npm run test:e2e-ai` (project này chỉ gom `test/e2e-ai/**`); chú thích mới nói đúng điều đó và chỉ đường `npm run test:e2e-ai` / `npm run measure:tokens -- --mode real-draft|real` cho lượt provider thật. `CLAUDE.md` mục Test mô tả 3 project (`unit`/`integration`/`e2e-ai`), `test/global-setup.ts` + `test/setup.ts` (Mongo in-memory), `AI_PROVIDER_OVERRIDE=mock`, `typecheck:test`. | Còn treo: nhánh `E2E_AI=1` di sản trong hai file e2e — giữ hay xoá/chuyển sang `test/e2e-ai/` là quyết định của T14/T18. |
