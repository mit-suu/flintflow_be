# Spec gaps

Thiếu sót / mâu thuẫn tài liệu phát hiện khi implement. Ai cũng được **thêm dòng**; không sửa dòng người khác.

| Ngày | Task | Tài liệu | Vấn đề | Cách xử lý tạm |
| --- | --- | --- | --- | --- |
| 2026-09-14 | T03 | Phases §8.2 vs `task-03-skill-assets.md` | Phases đếm 29 file (12 skill content). Task-03 liệt kê 13 content (thêm `product-brief` cho B-0…B-2, T20 sở hữu) ⇒ thực tế 30 SKILL.md (10 đủ nội dung, 20 stub). | Giữ 30; test `prompt-assets.test.ts` khoá số 30. Cần chốt lại con số trong Phases §8.2. |
| 2026-09-14 | T03 | Phases §8.2 | Tên file dạng `draft-to-ops.md` phẳng; task-03 chốt cấu trúc BMAD `action/draft-to-ops/SKILL.md` + `references/`. | Theo task-03. |
| 2026-09-14 | T08 | srs-spine §4.1 | Không nói "tồn tại" nghĩa là gì với khoá section (`sections[].id`, `addendum[].target_section`, `flags[].section_id`). Spine mới có `sections[]` rỗng nhưng addendum đã trỏ `fixed:5.4`. | Khoá section tồn tại khi phân giải được: `fixed:*` thuộc 20 section cố định, `feature:<id>`/`function:<id>` có phần tử. `reference-fields.ts` `sectionKeyExists`. Riêng `addendum[].target_section` chỉ cần đúng dạng (`sectionKeyWellFormed`): fixture minimal có entry trỏ `feature:F3` trước khi feature tồn tại. |
| 2026-09-14 | T08 | srs-spine §3 | Chỉ nêu cascade cho xoá màn/actor/feature. Chưa nói: xoá feature còn màn/function thì sao. | Không tự xoá (restrict): từ chối lô, trả `referrers`. Chính sách từng field ở `cascade.ts` `CASCADE_POLICY` và contract §0.2. |
| 2026-09-14 | T08 | srs-spine §6 | Bất biến 1, 2, 8 viết tuyệt đối thì Spine mới (mảng rỗng) bị từ chối mọi lô; 3–6 tuyệt đối thì Spine migrate có lỗi sẵn không sửa được gì. | 1, 2, 8 so trước/sau lô (chỉ chặn việc xoá); 3–6 chỉ chặn vi phạm mới. Lỗi có sẵn hiện qua cờ đỏ T09. |
| 2026-09-14 | T08 | srs-spine §6 bất biến 5 | `functions[].order` chỉ "duy nhất trong feature", không nói liên tục (fixture T02 lại kiểm liên tục). | Engine chỉ đòi duy nhất; xoá function không renumber (khớp case-03). `renumber functions[]` có sẵn nếu cần. |
| 2026-09-14 | T08 | srs-spine §2 `changes[]` | Không có chỗ lưu vị trí phần tử bị xoá và phân biệt "key không tồn tại" với `null` — revert không khôi phục deep-equal được. | Mã hoá trong `before`/`value`: `{_absent: true}` / `{_absent: true, index}` (op.types.ts `ABSENT`). |
| 2026-09-14 | T09 | srs-spine §5 | `stale` so `changes[].seq` với `accepted_at` (thời điểm) — khác đơn vị. Không nói change do chính step sở hữu ghi có làm stale không. | Mốc = `steps[].last_seq` lớn nhất của step accepted sở hữu s. Change có `step_id ∈ steps_of(s)` không làm s stale (status đã ra draft khi step đó chưa accepted). |
| 2026-09-14 | T09 | srs-spine §5 | `awaiting_reaccept` là "cờ phụ" nhưng không nói lưu ở đâu (lưu thì thành giá trị suy diễn). | Tính từ `steps[]`: có step sở hữu s ở `revision_requested`. T17 hoà giải đặt, gate Accept (T13) xoá. |
| 2026-09-14 | T09 | srs-spine §7.1 / fixture T02 | Fixture để `source_hash = "TBD"`; so hash thật thì mọi hình thành `diagram_stale` đỏ, trái DoD "fixture 0 cờ đỏ". | `source_hash` rỗng hoặc `TBD` = chưa tính ⇒ bỏ qua `diagram_stale`. T10 ghi hash bằng `computeSourceHash`. |
| 2026-09-14 | T09 | srs-spine §7 `section_empty` | Dự án không có non-screen function thì `fixed:3.1.4` luôn rỗng ⇒ cờ đỏ (waive được). Tài liệu không miễn. | Giữ cờ đỏ waive được; cần chốt có miễn `fixed:3.1.4` hay render "N/A". |
| 2026-09-14 | T09 | srs-spine §4 | `functions[].priority` (S-9.4) không có dòng riêng trong bảng §4. | Rơi vào dòng chung `functions[screen_id≠null|null]` ⇒ owner `function:<id>`. Không thêm dòng. |
| 2026-09-14 | T09 | Phases §6.4 / T12 | `steps_of(s)` cần step registry (T12) chưa có. | Bảng tạm `FIXED_OWNER_STEPS` trong `section-registry.ts`; function → `S-5.2`/`S-5.4@<screen|nonscreen>`. Thay khi T12 merge. |
| 2026-09-14 | T10 | srs-spine §2 `entities[].relations` | Chỉ lưu id đích, không có bản số; ERD cần crow's foot. | Mọi quan hệ vẽ `||--o{`. Cần thêm field bản số nếu muốn ERD đúng. |
| 2026-09-14 | T10 | Phases §7.1 context diagram | Nhãn tương tác trên cạnh (payment webhooks…) không thuộc `source_fields` §7.1. | Cạnh không nhãn; nếu cần nhãn phải bổ sung vào `source_fields` và Spine. |
| 2026-09-14 | T10 | plantuml-conventions (T03) vs `prompt-assets.test.ts` | Renderer đã có nội dung nhưng test T03 bắt mọi skill không phải action là `stub: true`. | Giữ `stub: true` + ghi chú trong SKILL.md; XREQ T10→T03 sửa test. |
| 2026-09-14 | T10 | Phases §7.1 usecase > 25 | Không nói id các phần khi tách hình. | `<base>-1`, `<base>-2`…; đổi số phần thì gỡ id cũ trong cùng transaction. |
| 2026-09-14 | T10 | compile-check.ts (giả định "HTTP 200 kèm ảnh lỗi") | Probe thật với `plantuml/plantuml-server:jetty` (1.2026.8): syntax sai ⇒ **HTTP 400** + ảnh lỗi. POST không có header chẩn đoán, GET có `x-plantuml-diagram-error(-line)`. Client cũ coi 400 là lỗi mạng và ném. | Client trả kết quả khi 400 + `image/*`; compile-check dùng header hoặc marker SVG, lấy dòng lỗi từ `[From string (line N)`. Có thể ghim tag 1.2026.8 (TODO(pin) trong docker-compose — T24). |
| 2026-09-14 | T11 | Phases §6.4 / task-11 | "Fields this step may write" cần step registry (T12). Ca op T02 cho thấy step xoá màn (S-4.1) phải gỡ cả `use_cases[].function_ids`, `sections[]`. | Bảng tạm `STEP_SPECS` (context-projection.ts), quyền ghi theo **gốc path mức phase** + `assumptions`. T12 thu hẹp theo step. |
| 2026-09-14 | T11 | skill draft-to-ops | Template không có placeholder cho tài liệu upload. | Nối `documents` vào cuối `content_guidance`; không đổi skill (T03). |
| 2026-09-14 | T11 | skill content (T03 stub) | 12 skill content còn `stub: true` (T14/T18 viết). | `draftOps` gửi hướng dẫn chung thay cho template stub. |
| 2026-09-14 | T11 | usage[] (spine.schema) | Task yêu cầu ghi `contextTokens` vào `usage[]` nhưng `usageSchema` (đóng băng) không có field này. | `draftOps` trả `contextTokens` + `usage[]` theo lượt (tokens_in/out, cost, logId); T13 ghi `tokens_in`. Đổi schema cần `contract-change`. |
| 2026-09-14 | T12 | Phases §4.2 / §6.4 | "Step tất định không Meter" chỉ liệt kê S-8.2, S-8.3, S-9.1, S-9.5; S-5.1 (lấy màn đầu queue), S-5.5 (sign-off), các step render không gọi model nhưng không có trong danh sách. | `deterministic: true` đúng 4 step của §4.2; step render/gate khác để T13 quyết khi có lượt gọi `render_fix`. |
| 2026-09-14 | T12 | Phases §6.2 / §6.4 | Tổng `51 + 5 × N` đếm mọi màn, nhưng màn `placeholder` không chạy vòng S-5 (fixture 19 màn chỉ có 5×6 step). | `totalSteps` giữ công thức tài liệu; `nextStep` bỏ qua vòng của màn placeholder. Thanh tiến độ vì vậy không chạm `total` khi có placeholder — cần chốt có trừ placeholder khỏi N không. |
| 2026-09-14 | T12 | task-12 `reads[]` | Chưa quy ước cách khai "step đọc tài liệu upload". | `reads` chứa token `documents` (thay cho `STEP_NEEDS_SOURCE_DOCUMENTS` tạm của T11 khi hợp nhất). |
| 2026-09-15 | T13 | `step-runner.service.ts` `pendingAnswers`, `meter.service.ts` in-process lock | Hàng chờ `answer_needed`/khoá step (`acquireStepLock`) giữ trong `Map`/`Set` bộ nhớ tiến trình — chỉ đúng khi chạy **một instance** Node. Nhiều instance (scale ngang) sẽ không thấy nhau, mỗi instance tự cho qua trần đồng thời. | Chấp nhận giới hạn single-instance cho tới khi có hàng đợi/khoá phân tán (Redis…) — ghi rõ trong comment đầu `step-runner.service.ts`. Không đổi thiết kế ở T13. |
| 2026-09-15 | T13 | `pipeline-contract.md` endpoint 6 `POST /steps/:stepId/gate`, `gateRequestSchema` | `gateRequestSchema` (đóng băng) không có `session_id` — `/gate` không xác nhận request tới từ session pipeline của project (khác `/run`, `/answer` đều có `NOT_PIPELINE_SESSION`). | Chưa sửa (đổi `gateRequestSchema` cần `contract-change`). `/gate` vẫn kiểm quyền sở hữu project qua `authorize()`. |
| 2026-09-15 | T13 | `pipeline-contract.md` endpoint 5 `POST /steps/:stepId/answer`, `stepAnswerRequestSchema` | `stepAnswerRequestSchema.answers[].answer` (`string \| string[]`, đóng băng) không giới hạn độ dài/số phần tử — client gửi answer khổng lồ vẫn được ghi thẳng vào transcript session. | Chưa giới hạn (đổi schema cần `contract-change`). Rủi ro thấp: chỉ user tự ghi transcript của chính mình. |
| 2026-09-15 | T15 | Product-Brief-to-SRS-Phases.md §6.3 | `functions[].order` là `.y` của số section §3.x.y không được nói rõ là "đánh riêng screen-bound/non-screen" hay là vị trí gộp — đọc thẳng `order` sinh số trùng khi một feature có cả hai loại function cùng `order`. | `.y` = vị trí 1-based trong danh sách đã gộp của feature theo đúng thứ tự `section-registry.listSections` (screen-bound trước, non-screen sau) — không phải `functions[].order` trực tiếp. `assemble.service.buildNumberMap`, test `buildNumberMap` (assemble.test.ts). |
| 2026-09-15 | T15 | srs-spine.md §4 / pipeline.dto.ts `assembleResponseSchema` | `sections` (đếm số section) không nói có tính heading nhóm tổng hợp (`group:*`, chèn lúc assemble, không phải section logic của contract) hay không. | Không tính `group:*` vào `sections` của `assembleResponseSchema` — `RenderedSection.id` có thêm tiền tố `group:` cho heading chương/mục cha (T15); T16 bỏ qua id có tiền tố này khi map flags/steps. Đề xuất ghi nhận tường minh vào `rendered-document.types.ts` qua PR `contract-change` khi có nhu cầu thật. |
| 2026-09-15 | T15 | task-15-assemble-export.md `fixed:I` | "dịch reason theo ngôn ngữ export nếu có" — Spine không lưu ngôn ngữ hiển thị của `reason`/không có nguồn dịch nào ở tầng dữ liệu hiện tại. | Chưa làm (Th3) — `reason` giữ nguyên ngôn ngữ đã ghi lúc tạo change. Cần chốt: dịch máy lúc export, hay bắt buộc ghi `reason` song ngữ lúc tạo change. |
| 2026-09-15 | T14 | `prompt-assets.test.ts` (T03) vs `assets/skills/content/*` | 4 content skill của T14 (project-classifier, product-overview, high-level-rules, actors-and-usecases) đã có nội dung thật nhưng vẫn phải giữ `stub: true` vì test T03 khoá "mọi skill không phải action = stub". | Giữ `stub: true` + ghi chú 1 dòng trong mỗi SKILL.md. XREQ T14→T03: tách assertion thành "action + 4 content skill T14 không stub"; T14 đặt `stub: false` trong cùng PR khi T03 sửa. |
| 2026-09-15 | T14 | `draft-to-ops.ts#contentGuidance` / `prompt-registry.service.ts#getSkill` | `draftOps` gọi `getSkill(ctx.skill)` không truyền `references`, nên `assets/skills/content/actors-and-usecases/references/*.md` không bao giờ tới model — mọi luật bắt buộc (actor kind, đặt tên use case, include/extend, sweep 6 chủ đề) phải nén thẳng vào `SKILL.md` (≤150 dòng) thay vì trỏ sang `references/`. | Nén luật vào `actors-and-usecases/SKILL.md`; `references/*.md` giữ làm tài liệu đọc thêm cho người, ghi rõ "not loaded at runtime". XREQ T14→T11: cho `draftOps` nạp `references: "all"` khi gọi `getSkill` cho skill content, để sau này có thể tách luật ra lại. |
| 2026-09-15 | T14 | `step-registry.json` S-2.5/S-3.1 vs `source-hash.ts#sourceProjection` (context diagram) | S-2.5 render sơ đồ ngữ cảnh từ `actors[kind!=human]`; S-3.1 (chạy sau) được phép thêm actor `kind=system|time`. Thêm actor phi-human ở S-3.1 luôn làm hình ngữ cảnh `diagram_stale` mà không step nào trong S-3 render lại "context" (chỉ S-2.5 render). | Chấp nhận cờ đỏ tạm thời; T14 minh hoạ cách đóng bằng `gate` `revision` + `accept` trên S-2.5 (reopen B7) trong `s2-s3.e2e.test.ts`. Đề xuất contract-change: S-3.6 renders thêm `["usecase", "context"]`, hoặc S-3.1 tự renders `["context"]` sau khi ghi actor. |
| 2026-09-15 | Quyết định Wave 3 | 5 contract-change + 6 XREQ treo sau phiên overnight (T13–T16) | Chặn DoD T13/T14 và M3. | **Đã chốt** (nhánh `wave3/decisions`): (1) `POST /resume` thành endpoint 24; (2) `/gate` bắt buộc `session_id` + kiểm `NOT_PIPELINE_SESSION`, `answers` ≤ 20 phần tử/≤ 4000 ký tự, `note` ≤ 2000; (3) `409 NO_WORKING_DRAFT {hint}` vào bảng lỗi; (4) `RenderedSection.id` `group:*` ghi vào contract; (5) S-3.6 `renders: ["usecase", "context"]` (một điểm render cuối S-3, không thêm render ở S-3.1). XREQ: `refundDeductedCredit` (T04) hoàn ví khi 409; bỏ `stub` cho 4 content skill T14 + 5 renderer T10; `PATCH /users/me {name, onboardedAt}` (modules/user); FE `ChatSession.is_pipeline`, `User.name/onboardedAt`. |
| 2026-09-15 | Quyết định Wave 3 | XREQ T14→T11 nạp `references/` | Nạp `references: "all"` cho skill content làm tăng token mỗi lượt Draft mà luật đã nén đủ trong `SKILL.md`. | **Từ chối**: giữ `SKILL.md` tự đủ (≤ 150 dòng), `references/*.md` chỉ là tài liệu cho người. Xem lại ở T18 nếu skill S-4…S-8 vượt 150 dòng. |
| 2026-09-15 | Quyết định Wave 3 | task-15 `fixed:I` "dịch reason" | Spine không lưu ngôn ngữ của `reason`. | **Chốt**: không dịch lúc export; `reason` giữ nguyên như lúc ghi change. Skill/route tạo change nên ghi `reason` tiếng Anh (luật render tiếng Anh). |
| 2026-09-15 | M3 (chạy thật) | `assemble.service.ts` (T15 review C3) | Sơ đồ chưa có PNG (fixture chưa render) ⇒ `POST /assemble` vẫn 200 nhưng không ghi cache ⇒ `GET /document`/`/export/word` 409 `NO_WORKING_DRAFT` — client không biết vì sao. | Tạm: chạy `POST /diagrams/all/render` trước assemble. Đề xuất T15: trả `images_missing[]` trong `assembleResponseSchema` (contract-change) hoặc cache kèm ảnh placeholder. |
| 2026-09-15 | M3 (chạy thật) | PlantUML local dev | `plantuml.jar -picoweb` trả ảnh "PlantUML PicoWebServer" cho POST `/svg`/`/png` ⇒ compile-check coi là ok, docx nhúng ảnh giả. | Dev không Docker: dùng shim chuyển tiếp GET `/svg|/png/<enc>` sang `/plantuml/...` và trả 405 cho POST. Production vẫn dùng `plantuml-server` (T24). |
| 2026-09-15 | M3 (chạy thật) | `glm.provider.ts` (GLM-5.3-Flash trên Modal) | `reasoning_effort: "none"` không tắt thinking ⇒ `content` có phần suy nghĩ + `</think>` ⇒ elicit `PARSE_FAILED` ngay S-1.2. | Sửa `db0defe`: gửi `thinking: {type: "disabled"}` + `stripReasoning`. |
| 2026-09-15 | Quyết định model | Skill S-2/S-3 (và mọi skill `provider: glm`) | Endpoint Modal chỉ có `zai-org/GLM-5.3-Flash`; `thinking: disabled` vẫn suy nghĩ ngầm tới hết token ở S-3.2 (run8 dừng 7/12). | **Chốt giữ GLM-5.3-Flash** (user 2026-09-15), gửi `reasoning_effort: "low"` + `response_format: json_object` (`7618fe7`, thay cách `thinking: disabled`). Probe prompt S-3.2: 13 s, ~250 token suy nghĩ, lô op hợp lệ. |
| 2026-09-15 | M3 (trình duyệt) | FE `ExportPanel.tsx` gọi `GET /projects/:id/baselines` (endpoint 20, T19) | BE chưa mount route ⇒ Express trả 404 HTML ⇒ panel hiện đỏ "Không kiểm tra được baseline: HTTP 404: Server returned non-JSON response". Không chặn tải bản nháp; tab "Word baseline" bị khoá. | Chờ T19 (Wave 4) hiện thực endpoint 20. Nếu cần sạch sớm: BE trả `[]` cho endpoint 20 trước khi có baseline. |
| 2026-09-15 | M3 (trình duyệt) | `export/word` + CORS (`src/app.ts`) | FE không đọc được `Content-Disposition` (CORS không `exposedHeaders`) ⇒ file tải về dùng tên dự phòng `M3 S-2_S-3 real provider (run18)-v0.58-draft.docx` thay tên BE `m3-s-2-s-3-real-provider-run18-v0.58-draft.docx`. | Đề xuất T24/T15: thêm `exposedHeaders: ["Content-Disposition"]` vào cấu hình CORS. |
| 2026-09-16 | T18 | `assets/step-registry.json` S-5.1, S-5.5 (đóng băng M2) | Hai step chỉ là sổ sách của vòng lặp (chọn màn kế tiếp, cổng ký duyệt) nhưng `deterministic: false` và S-5.1 còn được ánh xạ skill `function-detail` ⇒ mỗi màn tốn thêm 2 lượt gọi model cho việc không cần model. Đổi `deterministic` cần PR `contract-change`. | Runner bỏ qua Draft cho hai template này (`LOOP_BOOKKEEPING_TEMPLATES`, `step-runner.service.ts`); registry giữ nguyên. Nếu sau này mở PR `contract-change` thì đổi `deterministic: true` và gỡ hằng số này. |
| 2026-09-16 | T18 | Phases §6.2 "để lại màn" vs `gateRequestSchema` (đóng băng) | Không có hành động gate nào nghĩa là "để lại màn này, làm sau" — chỉ có accept / revision / regenerate / accept_as_is. | `accept_as_is` trên `S-5.1@<màn>` đặt `screens[].detail_status = placeholder` (`gate.service.loopScreenStatusOps`). Hợp lý về ngữ nghĩa: `accept_as_is` bắt buộc `note` và mở cờ vàng `accepted_as_is` — đúng là lời giải thích "vì sao để lại". Màn giữ nguyên khung `functions[]` nên không bắn `screen_pending_at_baseline`. |
| 2026-09-16 | T18 | `draft-to-ops.ts` `promptVariables` (vùng T11) | Chia lô ≤ 6 function cần nói cho model biết lô nào, nhưng thêm biến prompt phải sửa file của T11. | Thu hẹp chính `projection` của lượt đó (`step-runner.batchContext`) thay vì thêm biến: model chỉ **thấy** function trong lô. Bảo đảm mạnh hơn là dặn nó đừng đụng function khác. Không cần XREQ. |
| 2026-09-16 | T18 | srs-spine §1 op grammar vs task-18 "S-5.4 ghi `validations[]`" | `functions[].validations` là mảng phần tử có `id` ⇒ `set` cả mảng bị từ chối (`op_not_allowed`); phải `add` từng phần tử. Task-18 và bản nháp skill đều viết `set`. | Skill `function-detail` nêu rõ một `add` cho mỗi validation, fixture `s-5.4.json` làm đúng vậy. Phát hiện khi chạy e2e, không phải suy đoán. |
| 2026-09-16 | T18 | `assets/step-registry.json` S-7.1 `writes` vs traceability §4.1 | S-7.1 chỉ được ghi `business_rules`, `assumptions` nên KHÔNG thể nối ngược `functions[].business_rule_ids`. | Cạnh truy vết của §5.1 là `business_rules[].source_validation_ids` (một chiều). `functions[].business_rule_ids` chỉ được ghi ở S-5.4 cho rule đã tồn tại từ trước (tier `high` của S-2.4). Ghi rõ trong cả hai skill. |
| 2026-09-16 | T18 | `prompt-assets.test.ts` (T03) | Test khoá "mọi skill không phải action = stub"; 8 content skill của T18 viết thật thì phải bỏ `stub`. | XREQ T18→T03 theo đúng tiền lệ đã chốt 2026-09-15 cho T10/T14: thêm 8 thư mục vào `WRITTEN_NON_ACTION`. Chỉ còn `content/product-brief` là stub (T20 viết). |
